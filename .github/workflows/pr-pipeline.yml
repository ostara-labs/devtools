name: PR pipeline

# devtools self-adoption: the hub reviews its own PRs with the same
# pipeline it ships to the consumers (pia, bot, messenger-assistant,
# home, world-monitor-tui). The gate job calls the aggregate IN THIS
# repository (same-repo cross-ref, legal); it DUPLICATES the aggregate's
# own pull_request self-run for now -- the required "gate" context is
# reported by that self-run, and collapsing the duplication would move
# the required context to the composite "gate / gate" name, which is a
# ruleset change. Cleanup deferred until then.
#
# The pull_request event flows through github.event into every callee --
# no workflow_run, no context loss. Docs: docs/ai-review.md.

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, unlabeled]

concurrency:
  group: pr-pipeline-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  gate:
    name: gate
    permissions:
      contents: read
    uses: ostara-labs/devtools/.github/workflows/ci.yml@963c23b29239dfaee577cc391e2c77958a265092 # v1.12.0

  ai-review:
    name: ai-review
    needs: gate
    if: ${{ !cancelled() && needs.gate.result == 'success' && github.event_name == 'pull_request' && !github.event.pull_request.draft }}
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: ostara-labs/devtools/.github/workflows/ai-review.yml@963c23b29239dfaee577cc391e2c77958a265092 # v1.12.0
    secrets:
      AI_REVIEW_OPENROUTER_API_KEY: ${{ secrets.AI_REVIEW_OPENROUTER_API_KEY }}

  merge-gate:
    name: merge-gate
    needs: [gate, ai-review]
    if: ${{ !cancelled() && github.event_name == 'pull_request' && !github.event.pull_request.draft }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Verdict
        uses: ostara-labs/devtools/.github/actions/merge-gate-verdict@963c23b29239dfaee577cc391e2c77958a265092 # v1.12.0
        with:
          ci-result: ${{ needs.gate.result }}
          review-result: ${{ needs.ai-review.result }}
          draft: ${{ github.event.pull_request.draft }}
          labels: ${{ toJSON(github.event.pull_request.labels.*.name) }}
