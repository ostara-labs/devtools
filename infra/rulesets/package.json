{
  "name": "devtools-rulesets",
  "version": "1.0.0",
  "description": "GitHub Organization Rulesets for ostara-labs — server-side enforcement via Pulumi",
  "main": "index.ts",
  "scripts": {
    "preview": "pulumi preview",
    "up": "pulumi up",
    "destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/github": "^6.13.0",
    "@pulumi/pulumi": "^3.120.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
