{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "git-submodules": {
    "enabled": true
  },
  "github-actions": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "devtools submodule bumps: automerge — every devtools change is human-reviewed at the source repo, propagation is mechanical",
      "matchManagers": ["git-submodules"],
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "devtools workflow digest bumps: automerge — same trust model as submodule bumps",
      "matchManagers": ["github-actions"],
      "matchDepNames": ["/^ostara-labs\\/devtools/"],
      "automerge": true,
      "automergeType": "pr"
    }
  ]
}